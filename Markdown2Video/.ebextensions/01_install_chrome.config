container_commands:
  01_install_puppeteer_chrome:
    command: |
      export PUPPETEER_CACHE_DIR="/home/webapp/.cache/puppeteer"
      mkdir -p $PUPPETEER_CACHE_DIR
      npx puppeteer browsers install chrome-headless-shell
      chown -R webapp:webapp /home/webapp/.cache
    leader_only: true
